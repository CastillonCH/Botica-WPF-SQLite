<Window x:Class="Botica.App.Views.LaboratoriosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Laboratorios" Height="500" Width="700"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0" ItemsSource="{Binding Laboratorios}"
                  SelectedItem="{Binding LaboratorioSeleccionado, Mode=TwoWay}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" />
                <DataGridCheckBoxColumn Header="Activo" Binding="{Binding Activo}" Width="60" />
            </DataGrid.Columns>
        </DataGrid>

        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Nombre" Margin="0,0,0,4" />
                <TextBox Text="{Binding LaboratorioSeleccionado.Nombre, UpdateSourceTrigger=PropertyChanged}" />

                <CheckBox Content="Activo" IsChecked="{Binding LaboratorioSeleccionado.Activo}" Margin="0,12,0,0" />

                <TextBlock Text="{Binding Mensaje}" Foreground="Red" TextWrapping="Wrap" Margin="0,12,0,0" />

                <Button Content="Nuevo" Command="{Binding NuevoCommand}" Margin="0,16,0,4" Height="32"
                        Style="{StaticResource Style.SecondaryButton}" />
                <Button Content="Guardar" Command="{Binding GuardarCommand}" Margin="0,0,0,4" Height="32" />
                <Button Content="Eliminar" Command="{Binding EliminarCommand}" Height="32"
                        Style="{StaticResource Style.DangerButton}" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
