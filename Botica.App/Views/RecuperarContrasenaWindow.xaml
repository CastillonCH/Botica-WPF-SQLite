<Window x:Class="Botica.App.Views.RecuperarContrasenaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Botica.App.Controls"
        mc:Ignorable="d"
        Title="Recuperar contraseña" Height="480" Width="400"
        MinHeight="440" MinWidth="360"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="Style.OcultarSiCodigoEnviado" TargetType="Button" BasedOn="{StaticResource Style.BaseButton}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding CodigoEnviado}" Value="True">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="Style.MostrarSiCodigoEnviado" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding CodigoEnviado}" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Recuperar contraseña" FontSize="16" FontWeight="Bold" Margin="0,0,0,4" />
            <TextBlock Text="Ingresa tu usuario y te enviaremos un código de verificación por correo."
                       Foreground="{StaticResource Brush.TextSecondary}" TextWrapping="Wrap" Margin="0,0,0,16" />

            <TextBlock Text="Usuario" Margin="0,0,0,4" />
            <TextBox Text="{Binding NombreUsuario, UpdateSourceTrigger=PropertyChanged}" />

            <Button Content="Enviar código" Command="{Binding EnviarCodigoCommand}" Margin="0,12,0,0" Height="36"
                    Style="{StaticResource Style.OcultarSiCodigoEnviado}" />

            <StackPanel Style="{StaticResource Style.MostrarSiCodigoEnviado}">
                <TextBlock Text="Código recibido por correo" Margin="0,16,0,4" />
                <TextBox Text="{Binding Codigo, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Nueva contraseña" Margin="0,12,0,4" />
                <controls:PasswordRevealBox Password="{Binding NuevaContrasena, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Confirmar nueva contraseña" Margin="0,12,0,4" />
                <controls:PasswordRevealBox Password="{Binding ConfirmarContrasena, UpdateSourceTrigger=PropertyChanged}" />

                <Button Content="Restablecer contraseña" Command="{Binding RestablecerCommand}" Margin="0,16,0,0"
                        Height="36" />

                <Button Content="Reenviar código" Command="{Binding EnviarCodigoCommand}" Margin="0,8,0,0" Height="32"
                        Style="{StaticResource Style.SecondaryButton}" />
            </StackPanel>

            <TextBlock Text="{Binding Mensaje}" Foreground="{StaticResource Brush.Primary}" TextWrapping="Wrap"
                       Margin="0,16,0,0" />
        </StackPanel>
    </ScrollViewer>
</Window>
