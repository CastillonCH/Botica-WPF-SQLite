<Window x:Class="Botica.App.Views.AuditoriaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Auditoría" Height="600" Width="800"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <DataGrid ItemsSource="{Binding Eventos}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding FechaHora, StringFormat={}{0:g}}" Width="130" />
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="150" />
                <DataGridTextColumn Header="Acción" Binding="{Binding Accion}" Width="150" />
                <DataGridTextColumn Header="Detalle" Binding="{Binding Detalle}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
