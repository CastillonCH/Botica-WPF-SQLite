<Window x:Class="Botica.App.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Botica - Iniciar sesión"
        Height="360" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="24" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Botica" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" />

        <StackPanel Grid.Row="2">
            <TextBlock Text="Usuario" Margin="0,0,0,4" />
            <TextBox x:Name="UsuarioTextBox" Text="{Binding NombreUsuario, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock Text="Contraseña" Margin="0,0,0,4" />
            <PasswordBox x:Name="PasswordBox" KeyDown="PasswordBox_KeyDown" />
        </StackPanel>

        <TextBlock Grid.Row="6" Text="{Binding MensajeError}" Foreground="Red" TextWrapping="Wrap" />

        <Button Grid.Row="8" Content="Ingresar" Height="36" Click="IngresarButton_Click" />
    </Grid>
</Window>
