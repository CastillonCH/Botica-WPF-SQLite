<Window x:Class="Botica.App.Views.CategoriasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Categorías" Height="500" Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0" ItemsSource="{Binding Categorias}"
                  SelectedItem="{Binding CategoriaSeleccionada, Mode=TwoWay}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" />
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="2*" />
                <DataGridCheckBoxColumn Header="Activo" Binding="{Binding Activo}" Width="60" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Column="2">
            <TextBlock Text="Nombre" Margin="0,0,0,4" />
            <TextBox Text="{Binding CategoriaSeleccionada.Nombre, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Descripción" Margin="0,12,0,4" />
            <TextBox Text="{Binding CategoriaSeleccionada.Descripcion, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" Height="60" TextWrapping="Wrap" />

            <CheckBox Content="Activo" IsChecked="{Binding CategoriaSeleccionada.Activo}" Margin="0,12,0,0" />

            <TextBlock Text="{Binding Mensaje}" Foreground="Red" TextWrapping="Wrap" Margin="0,12,0,0" />

            <Button Content="Nuevo" Command="{Binding NuevoCommand}" Margin="0,16,0,4" Height="32" />
            <Button Content="Guardar" Command="{Binding GuardarCommand}" Margin="0,0,0,4" Height="32" />
            <Button Content="Eliminar" Command="{Binding EliminarCommand}" Height="32" />
        </StackPanel>
    </Grid>
</Window>
