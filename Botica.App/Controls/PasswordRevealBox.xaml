<UserControl x:Class="Botica.App.Controls.PasswordRevealBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="Style.LinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="Foreground" Value="{StaticResource Brush.Primary}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}"
                                   FontSize="{TemplateBinding FontSize}" FontWeight="{TemplateBinding FontWeight}"
                                   VerticalAlignment="Center" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource Brush.PrimaryDark}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <PasswordBox x:Name="InternalPasswordBox" Padding="10,7,58,7"
                     PasswordChanged="InternalPasswordBox_PasswordChanged" />
        <TextBox x:Name="InternalTextBox" Padding="10,7,58,7" Visibility="Collapsed"
                 TextChanged="InternalTextBox_TextChanged" />
        <Button x:Name="VerButton" Content="Mostrar" Style="{StaticResource Style.LinkButton}"
                Margin="0,0,10,0" FontSize="11" FontWeight="SemiBold" Click="VerButton_Click" Focusable="False" />
    </Grid>
</UserControl>
